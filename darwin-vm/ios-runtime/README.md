# DarwinVM iOS Runtime

Experimental iPhone/iPad host runtime combining the VibeContainers shell with a vphone-derived Apple guest execution core.

This target is deliberately iOS/iPadOS-only and standalone. It is not a controller for a Windows build and does not expose a generic VM-creation UI.

See `ARCHITECTURE.md` for the current integration boundary and milestone order.
