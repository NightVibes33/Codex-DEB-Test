name: Dispatch Final Gif2Ani 3.4 Proof

on:
  push:
    branches: [main]
    paths:
      - triggers/dispatch-gif2ani-340-final.txt
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: gif2ani-340-final-dispatch
  cancel-in-progress: false

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch isolated direct-device proof
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run gif2ani-340-direct-device-proof-20260725.yml --repo "$GITHUB_REPOSITORY" --ref main
